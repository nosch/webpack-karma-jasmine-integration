# Webpack, Karma and Jasmine Integration

Very simple dev stack:

    $ npm install
    $ npm start   # webpack dev server
    $ npm test   # karma single run
    $ npm run karma   # karma watcher
    $ npm run dist   # webpack "release" build
